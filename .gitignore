# Dependencies (root + workspaces)
node_modules/

# OS / editor
.DS_Store
*.pem

# Next.js (web)
web/.next/
web/out/
web/.vercel

# NestJS (api)
api/dist/

# Secrets — never commit (examples are tracked explicitly)
.env
.env.local
api/.env
web/.env
web/.env.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# TypeScript
*.tsbuildinfo

# Local tooling
.claude/
