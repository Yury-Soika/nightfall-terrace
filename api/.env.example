DATABASE_URL=postgres://postgres:postgres@localhost:5432/reservenow

# First-time admin user (seed runs once if no user exists with this email). Do not commit real passwords.
ADMIN_EMAIL=admin@nightfall.local
ADMIN_PASSWORD=change-me

JWT_SECRET=change-me
JWT_EXPIRES_IN=7d

STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

APP_URL=http://localhost:3000

